/*
 * Add query methods like this:
 *  Hashtags.findPublic = function () {
 *    return Hashtags.find({is_public: true});
 *  }
 */

/*
Hashtags.findSuggestions = function(usedHashtags) {
  return Hashtags.find( { name: { $nin: usedHashtags } });
}

Hashtags.findSuggestionsCount = function(usedHashtags) {
  return Hashtags.find( { name: { $nin: usedHashtags } }).count();
}

Hashtags.findHashtag = function(id) {
  return Hashtags.findOne({_id : id });
}*/